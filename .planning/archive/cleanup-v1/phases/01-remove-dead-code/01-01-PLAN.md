---
phase: 01-remove-dead-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/App.tsx, src/pages/VariantA.tsx, src/pages/VariantB.tsx]
autonomous: true

must_haves:
  truths:
    - "Site at / works exactly as before"
    - "No /a or /b routes exist"
    - "No unused variant files exist"
  artifacts:
    - path: "src/App.tsx"
      provides: "Routing with only Home"
      contains: "Route path=\"/\""
    - path: "src/pages/Home.tsx"
      provides: "Oracle implementation"
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/Home.tsx"
      via: "import and Route element"
      pattern: "import.*Home.*from.*pages/Home"
---

<objective>
Delete unused variant files and clean up routing.

Purpose: Remove dead code (VariantA.tsx, VariantB.tsx) and simplify App.tsx to only route to Home.
Output: Cleaner codebase with only the live variant remaining.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Relevant source files:
@src/App.tsx
@src/pages/VariantA.tsx
@src/pages/VariantB.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete variant files</name>
  <files>src/pages/VariantA.tsx, src/pages/VariantB.tsx</files>
  <action>Delete both files using rm or git rm:
  - src/pages/VariantA.tsx
  - src/pages/VariantB.tsx
  These are unused variants - only Home.tsx serves the live site.</action>
  <verify>ls src/pages/ shows only Home.tsx</verify>
  <done>VariantA.tsx and VariantB.tsx no longer exist</done>
</task>

<task type="auto">
  <name>Task 2: Clean up App.tsx routing</name>
  <files>src/App.tsx</files>
  <action>Edit App.tsx to:
  1. Remove imports for VariantA and VariantB
  2. Remove Route elements for /a and /b
  3. Keep only the / route pointing to Home
  4. Keep Analytics component unchanged

  Result should be:
  - Import only Home from ./pages/Home
  - Single Route for path="/" element={<Home />}
  - Analytics stays at end of BrowserRouter</action>
  <verify>npm run build succeeds with no errors</verify>
  <done>App.tsx has single route, no unused imports</done>
</task>

<task type="auto">
  <name>Task 3: Verify site works</name>
  <files></files>
  <action>Run npm run build to verify:
  1. TypeScript compiles without errors
  2. No missing imports or references
  3. Build completes successfully</action>
  <verify>npm run build exits with code 0</verify>
  <done>Build passes, site ready for deployment</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds
- [ ] src/pages/ contains only Home.tsx
- [ ] App.tsx has no references to VariantA or VariantB
- [ ] Only one Route element exists (path="/")
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- VariantA.tsx and VariantB.tsx deleted
- App.tsx simplified to single route
</success_criteria>

<output>
After completion, create `.planning/phases/01-remove-dead-code/01-01-SUMMARY.md`
</output>
